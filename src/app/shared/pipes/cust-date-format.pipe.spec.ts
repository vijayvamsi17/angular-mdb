import { CustDateFormatPipe } from './cust-date-format.pipe';

describe('CustDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CustDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
